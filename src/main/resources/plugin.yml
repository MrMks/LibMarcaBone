name: DevToolsB
main: com.github.mrmks.mc.marcabone.Marcabone
version: ${version}
commands:
  dtbr:
    description: reload build-in language files
    usage: <command>
    permission: dtb.perm.reload
permissions:
  dtb.perm.reload:
    default: op